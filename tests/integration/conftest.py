"""Integration test configuration for Fence AI."""
import sys
import os

# Add the parent tests directory to path to access shared fixtures
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
